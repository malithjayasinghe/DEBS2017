package org.wso2.siddhi.debs2017;

import org.wso2.siddhi.core.event.Event;

import java.util.ArrayList;

/**
 * Created by sachini on 3/29/17.
 */
public class SortingTestCase {
  /*  private static ArrayList<Integer> node0 = new ArrayList<>();
    private static ArrayList<Integer> node1 = new ArrayList<>();
    private static ArrayList<Integer> node2 = new ArrayList<>();
    private static int[] arrivedEvents = new int[3];
    private  int currentEvent ;
    private synchronized void sortList(){
        //currentEvent = node0.get(0);
        arrivedEvents[0] = node0.get(0);
        arrivedEvents[1] = node1.get(0);
        arrivedEvents[2] = node2.get(0);
        currentEvent = arrivedEvents[0];
        int currentnode=0;
        for(int i =0; i < arrivedEvents.length; i++){
            for (int j = i+1; j<arrivedEvents.length-1;j++){
             //   if(getTime(currentEvent)>getTime(arrivedEvents[j])){
                    currentEvent = arrivedEvents[j];
                    currentnode = j;
                }
            }
        }

        if(currentnode == 0) {
            node0.remove(0);
        }
        else if(currentnode == 1 ) {
            node1.remove(0);
        }
        else {
            node2.remove(0);
        }
        System.out.println(currentEvent);

    }*/
}
